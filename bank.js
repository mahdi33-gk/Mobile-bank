document.getElementById('add-money').addEventListener('click', function(event){
    event.preventDefault();
    const bankId = inputValue('add-moneyInput');
    const bankPin = inputValue('add-pinInput');
})